import 'package:flutter/material.dart';

class Save_scrre extends StatefulWidget {
  const Save_scrre({Key? key}) : super(key: key);

  @override
  State<Save_scrre> createState() => _Save_scrreState();
}

class _Save_scrreState extends State<Save_scrre> {
  @override
  Widget build(BuildContext context) {
    return const Placeholder();
  }
}
